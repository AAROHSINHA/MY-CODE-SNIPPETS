#include <stdio.h>

/*
1. Heap sort is an optimization over the selection sort. In selection sort, before swapping
we find the max element in O(n) time and then sort. This leads to O(n square) complexity.
But in heap sort, we find the max element in O(logn) time, hence leading to O(nlogn) complexity
in total
*/

int main(){

}
